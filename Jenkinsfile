@Library('my-shared-lib') _

def call(args){
    pipeline {
        agent any 
        stages { 
            stage('Build') {
                steps {
                    echo 'build'
                }
            }
        }
    }
}
    
