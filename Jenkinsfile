@Library('my-shared-lib') _

    pipeline {
        agent any 
        stages { 
            stage('checkout') {
                steps {
                    nodejs ()
                }
            }
		}
    }
